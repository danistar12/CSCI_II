{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "cl.exe",
            "args": [
                "-g",
                "${workspaceFolder}/Facts.cpp",
                "-o",
                "${workspaceFolder}/Facts",
                "-I",
                "C:/sqlite", // Path to the directory containing sqlite3.h
                "-L",
                "C:/sqlite", // Path to the directory containing sqlite3.lib or sqlite3.dll
                "-lsqlite3"  // Link with the SQLite library
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"],
            "detail": "Generated task by Debugger."
        }
    ]
}